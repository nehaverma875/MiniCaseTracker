{
  "name": "mini-case-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "free:ports": "lsof -ti:3500,5001,5173,5174 | xargs kill -9 2>/dev/null || true",
    "predev": "npm run free:ports",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "preview": "npm run free:ports && npm run build --prefix client && concurrently \"npm start --prefix server\" \"npm run preview --prefix client\"",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
